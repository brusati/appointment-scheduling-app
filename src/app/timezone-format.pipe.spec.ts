import { TimezoneFormatPipe } from './timezone-format.pipe';

describe('TimezoneFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new TimezoneFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
